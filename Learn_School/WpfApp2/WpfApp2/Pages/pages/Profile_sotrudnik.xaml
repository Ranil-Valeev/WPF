<Page x:Class="WpfApp2.Pages.Profile_sotrudnik"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Profile_sotrudnik">

    <Grid Background="#FFE7">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Профиль" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.Row="0" />
            <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Width="400">
                <TextBlock Text="Фамилия:" FontWeight="Bold"/>
                <TextBox Text="{Binding Фамилия}" IsReadOnly="True"/>
                <TextBlock Text="Имя:" FontWeight="Bold"/>
                <TextBox Text="{Binding Имя}" IsReadOnly="True"/>
                <TextBlock Text="Отчество:" FontWeight="Bold"/>
                <TextBox Text="{Binding Отчество}" IsReadOnly="True"/>
                <TextBlock Text="Серия номер:" FontWeight="Bold"/>
                <TextBox Text="{Binding Серия_номер}" IsReadOnly="True"/>
                <TextBlock Text="Дата рождения:" FontWeight="Bold"/>
                <TextBox Text="{Binding Дата_рождения}" IsReadOnly="True"/>
                <TextBlock Text="Пол:" FontWeight="Bold"/>
                <TextBox Text="{Binding Пол}" IsReadOnly="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

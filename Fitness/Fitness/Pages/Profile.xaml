<Page x:Class="Fitness.Pages.Profile"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Fitness.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="540" d:DesignWidth="540"
      Title="Profile">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="#ADD8E6" CornerRadius="10" Padding="20" Margin="0,0,270,270" Grid.ColumnSpan="2" Grid.RowSpan="6">
            <Grid Margin="-2,0,0,0" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Left" Width="231">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Margin="0,0,-196,-2">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5" HorizontalAlignment="Left" Width="245">
                        <TextBlock Text="{Binding Логин}" x:Name="Login" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2" HorizontalAlignment="Left" Width="245">
                        <TextBlock Text="{Binding Email}" x:Name="Email" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5" Width="245" HorizontalAlignment="Left">
                        <TextBlock Text="{Binding Дата_регистрации}" x:Name="Date" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>

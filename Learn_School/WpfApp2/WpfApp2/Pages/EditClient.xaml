<Page x:Class="WpfApp2.Pages.EditClient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditClient">

    <Grid  Background="#FFE7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <ComboBox x:Name="ClientSelector" 
          SelectionChanged="ClientSelector_SelectionChanged"
          Width="250" Height="25" Margin="0,0,0,10">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Фамилия}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Имя}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>


        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Top" Width="300">

            <TextBlock Text="Фамилия"/>
            <TextBox x:Name="LastNameBox" Margin="0,5"/>

            <TextBlock Text="Имя"/>
            <TextBox x:Name="FirstNameBox" Margin="0,5"/>

            <TextBlock Text="Отчество"/>
            <TextBox x:Name="MiddleNameBox" Margin="0,5"/>

            <TextBlock Text="Телефон"/>
            <TextBox x:Name="PhoneBox" Margin="0,5"/>

            <TextBlock Text="Email"/>
            <TextBox x:Name="EmailBox" Margin="0,5"/>

            <TextBlock Text="Дата рождения"/>
            <DatePicker x:Name="BirthDatePicker" Margin="0,5"/>

            <TextBlock Text="Пол"/>
            <ComboBox x:Name="GenderBox" Margin="0,5">
                <ComboBoxItem Content="Мужской"/>
                <ComboBoxItem Content="Женский"/>
            </ComboBox>

            <TextBlock Text="Логин"/>
            <TextBox x:Name="LoginBox" Margin="0,5"/>

            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,5"/>

            <Button Content="Сохранить" Click="Save_Click" Margin="0,10,0,0" Background="LightGreen"/>
        </StackPanel>
    </Grid>
</Page>

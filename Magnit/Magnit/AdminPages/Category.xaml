<Page x:Class="Magnit.AdminPages.Category"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Magnit.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Category">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="cbCategories" Grid.Row="0" Margin="0,0,0,10"
                 DisplayMemberPath="название"
                 SelectionChanged="cbCategories_SelectionChanged"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Название категории:"/>
            <TextBox x:Name="txtName" MaxLength="50"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Margin="0,0,10,0" Click="btnSave_Click"/>
            <Button x:Name="btnNew" Content="Новая категория" Width="120" Click="btnNew_Click"/>
        </StackPanel>
    </Grid>
</Page>

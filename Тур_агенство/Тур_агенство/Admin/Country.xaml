<Page x:Class="Тур_агенство.Admin.Country"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Тур_агенство.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Country">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
            <Label Content="Выберите страну:" VerticalAlignment="Center"/>
            <ComboBox x:Name="CountryComboBox" Width="300" Margin="10,0,0,0"
                  DisplayMemberPath="Название" SelectionChanged="CountryComboBox_SelectionChanged"/>
        </StackPanel>

        <Label Grid.Row="1" Content="Название страны:"/>
        <TextBox x:Name="CountryNameTextBox" Grid.Row="2" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Width="120" Margin="5" Background="#2C3E50" Foreground="White" Click="SaveButton_Click"/>
            <Button Content="Новый" Width="120" Margin="5" Background="#2C3E50" Foreground="White" Click="NewButton_Click"/>
            <Button Content="Удалить" Width="120" Margin="5" Background="#2C3E50" Foreground="White" Click="DeleteButton_Click"/>
        </StackPanel>
    </Grid>
</Page>

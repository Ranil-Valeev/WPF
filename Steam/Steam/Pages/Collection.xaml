<Page x:Class="Steam.Pages.Collection"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Steam.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="540" d:DesignWidth="540"
      Title="Collection">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="CollectionDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="CollectionDataGrid_SelectionChanged"
                  MouseDoubleClick="CollectionDataGrid_MouseDoubleClick"
                  RowHeight="80">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Название}" Width="150"/>
                <DataGridTextColumn Header="Жанры" Binding="{Binding Жанры}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Статус}" Width="100"/>
                <DataGridTemplateColumn Header="Фото" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Фото}" Width="70" Height="70" Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Margin="0,10,0,0" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,5">
                <TextBlock Text="Изменить статус:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="StatusComboBox"
                          Width="150"
                          SelectionChanged="StatusComboBox_SelectionChanged"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                <Button x:Name="AddGameButton"
                        Content="Добавить игру"
                        Width="150"
                        Margin="5"
                        Click="AddGameButton_Click"/>
                <Button x:Name="LeaveReviewButton"
                        Content="Оставить отзыв"
                        Width="150"
                        Margin="5"
                        Click="LeaveReviewButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>

<Page x:Class="Magnit.AdminPages.Product"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Magnit.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Product">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="cbProducts" Grid.Row="0" Margin="0,0,0,10"
                 DisplayMemberPath="название"
                 SelectionChanged="cbProducts_SelectionChanged"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Название товара:"/>
            <TextBox x:Name="txtName" MaxLength="50"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Label Content="Категория:"/>
            <ComboBox x:Name="cbCategories" DisplayMemberPath="название"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <Label Content="Производитель:"/>
            <ComboBox x:Name="cbManufacturers" DisplayMemberPath="Название"/>
        </StackPanel>
        <StackPanel Grid.Row="4">
            <Label Content="Описание:"/>
            <TextBox x:Name="txtDescription" AcceptsReturn="True" 
                    VerticalScrollBarVisibility="Auto" MaxLength="500"/>
        </StackPanel>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Margin="0,0,10,0" Click="btnSave_Click"/>
            <Button x:Name="btnNew" Content="Новый товар" Width="100" Click="btnNew_Click"/>
        </StackPanel>
    </Grid>
</Page>

<Page x:Class="WpfApp2.Pages.RecordClient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RecordClient">

    <Grid>
        <DataGrid x:Name="RecordsGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" Background="{StaticResource GradientBackground}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Услуга" Binding="{Binding Рабочее_время.Услуги.Название}" Width="*"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Рабочее_время.Дата}" Width="*"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding Рабочее_время.Сотрудник.ФИО}" Width="*"/>
                <DataGridTextColumn Header="Стоимость" Binding="{Binding Стоимость}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Статус_записи1.Название}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>

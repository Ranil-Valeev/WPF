<Window x:Class="Тур_агенство.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Тур_агенство"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStyle="None"
        ResizeMode="NoResize"
        Background="#f6f6f6">
    <Grid>
        <Border Background="#2C3E50" Height="40" VerticalAlignment="Top">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="icon.png" Width="24" Height="24" Margin="10,0,5,0"/>
                    <TextBlock Text="Турагентство" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Button Width="30" Height="30" Background="Transparent" BorderThickness="0" Click="Minimize_Click" Cursor="Hand">
                        <Image Source="minimize_window.png"/>
                    </Button>
                    <Button Width="30" Height="30" Background="Transparent" BorderThickness="0" Click="Close_Click" Cursor="Hand">
                        <Image Source="close_window.png"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Margin="0,40,0,0">

            <Border Background="White" CornerRadius="15" Width="400" Height="350"
                    HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30" 
                    >
                <StackPanel>

                    <Image Source="icon.png" Width="60" Height="60" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <TextBlock Text="Авторизация" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" 
                               HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <TextBlock Text="Логин (почта)" FontSize="14" Foreground="#555"/>
                    <TextBox x:Name="LoginBox" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Text="Пароль" FontSize="14" Foreground="#555"/>
                    <PasswordBox x:Name="PasswordBox" Height="30" Margin="0,0,0,20"/>

                    <Button Content="Войти" Height="35" Background="#2C3E50" Foreground="White" BorderThickness="0" 
                            Click="LoginButton_Click" Cursor="Hand"/>

                    <TextBlock Text="Нет аккаунта?" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                    <Button Content="Зарегистрироваться" Click="RegisterButton_Click"
                            Foreground="#2C3E50" Background="Transparent" BorderThickness="0" 
                            FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

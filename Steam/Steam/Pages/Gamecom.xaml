<Page x:Class="Steam.Pages.Gamecom"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Steam.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Gamecom">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="NameCom" Grid.Row="0" 
        FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        <DataGrid x:Name="ServicesGrid" Grid.Row="2" 
     AutoGenerateColumns="False" IsReadOnly="True"
     HeadersVisibility="Column" GridLinesVisibility="None"
     SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Игра" Binding="{Binding Название}" Width="*"/>
                <DataGridTextColumn Header="Платформы" Binding="{Binding Платформы}" Width="Auto"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Описание}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
